apiVersion: v1
kind: Service
metadata:
  name: mysql-service   # это имя будет использовано в phpMyAdmin как PMA_HOST
spec:
  selector:
    app: mysql
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
